@page
@model DeletePersonalDataModel
@{
    ViewData["Title"] = "Изтрий личните данни";
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
}

<h4>@ViewData["Title"]</h4>

<div class="alert alert-warning" role="alert">
    <p>
        <strong>Изтривайки тези данни, Вашият акаунт ще бъде изтрит и това действие е не обратимо.</strong>
    </p>
</div>

<div>
    <form id="delete-user" method="post" class="form-group">
        <div asp-validation-summary="All" class="text-danger"></div>
        @if (Model.RequirePassword)
        {
            <div class="form-group">
                <label asp-for="Input.Password"> Парола </label>
                <input asp-for="Input.Password" class="form-control" />
                <span asp-validation-for="Input.Password" class="text-danger"></span>
            </div>
        }
        <button class="btn btn-outline-danger" type="submit">Изтрий данните и закрий акаунта!</button>
    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}